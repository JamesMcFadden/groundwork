# Copy to k8s/secrets.env, which is gitignored, and replace every value; `openssl rand -hex 32`
# makes one. `kubectl apply -k k8s` reads the file into the groundwork-secrets Secret.
POSTGRES_PASSWORD=change-me
S3_SECRET_KEY=change-me
